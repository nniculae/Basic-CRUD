{% extends 'base.html.twig' %}

{% block title %}
    Update Product
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a  class="btn btn-primary" href="{{ path('category-list') }}">Go Back</a>
{% endblock %}